---
- name: Create a file in the user's home directory
  hosts: localhost
  connection: local
  gather_facts: yes

  tasks:
    - name: Ensure x.txt exists in the home directory
      ansible.builtin.file:
        path: "{{ ansible_env.HOME }}/x.txt"
        state: touch
        mode: '0644'
